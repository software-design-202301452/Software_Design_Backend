apiVersion: v1
kind: Service
metadata:
  name: software-design-backend
  labels:
    app: software-design-backend
spec:
  type: LoadBalancer
  selector:
    app: software-design-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
